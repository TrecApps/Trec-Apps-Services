spring.datasource.url=${DB_URL}

# Register with Eureka
spring.application.name: resource-service
eureka.client.fetch-registry: false
eureka.client.service-url.defaultZone: http://localhost:8081/eureka
eureka.instance.lease-renewal-interval-in-seconds: 5
eureka.instance.lease-expiration-duration-in-seconds: 10

#${DB_PASSWORD}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=${DB_DRIVER}

#hibernate config
spring.jpa.database-platform=${DB_DIALECT}

hibernate.enable_lazy_load_no_trans=false

spring.jpa.hibernate.ddl-auto=update

# spring.jackson.serialization.fail-on-empty-beans=false

# Keys
# trec.key.public=${TREC_PUBLIC_KEY}

# Mail 
#email
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=${EM_USERNAME}
# spring.mail.password=${EM_PASSWORD}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true

# Client/Auth server interaction
# trec.apps.client.id=${FALSEHOOD_CLIENT_ID}
# trec.apps.user.service.url=${USER_SERVICE_URL}